import React from 'react';
import styled from 'styled-components';

const Title = styled.h1`
  text-align: center;
`;

const PageTitle = () => <Title>Github Profile Card App</Title>;

export default PageTitle;
